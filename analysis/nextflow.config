params {
    // Default resources - optimized for standard design runs
    cpus = 4
    memory = '4 GB'
}

profiles {
    standard {
        process.executor = 'local'
    }
}

// Ensure processes know to use the generic resource params
process {
    cpus = { params.cpus }
    memory = { params.memory }
}